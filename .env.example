# Supabase project credentials
# Copy this file to .env.local and fill in your values from:
# https://supabase.com/dashboard → Project Settings → API

NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
ADMIN_PASSWORD=your-admin-password

# CMS admin login (for /cms)
CMS_ADMIN_USERNAME=admin
CMS_ADMIN_PASSWORD=change-this-password
CMS_ADMIN_SESSION_TOKEN=change-this-session-token
# Recovery token for /cms/recover — must be at least 16 characters.
# Set this, restart the dev server, then visit /cms/recover to reset your username/password.
# Leave blank (or omit) to disable the recovery page.
CMS_RECOVERY_TOKEN=

# Buildstate Module Visibility Flags (SiteSign-First Repositioning)
NEXT_PUBLIC_SITESIGN_PRIMARY=true
NEXT_PUBLIC_SHOW_PLANNER_PRIMARY=false
NEXT_PUBLIC_SHOW_ROADMAP_MODULES=false

# Anthropic API key — server-side only, never expose to the browser
# Used by the ITP generate route (app/api/itp-generate/route.ts)
ANTHROPIC_API_KEY=your-key-here  # Required for SiteITP AI checklist generation

# Upstash Redis — used by the ITP sign route for distributed rate limiting.
# Get these values from https://console.upstash.com → your Redis database → REST API.
# If omitted the route falls back to allowing all requests (no rate limiting).
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-rest-token
